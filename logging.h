
//Define this to enable logging (it also enables the report the times of important steps of the algorithm)
#define ENABLE_LOGGING





#ifdef ENABLE_LOGGING
#define LOGGING(x) x
#else
#define LOGGING(x) 
#endif